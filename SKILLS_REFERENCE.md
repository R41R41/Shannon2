# Minebot スキルリファレンス

42 個の原子的スキル一覧

---

## 🏃 移動・視線（7 個）

| スキル          | 主要パラメータ | 用途              |
| --------------- | -------------- | ----------------- |
| `move-to`       | x, y, z, range | 指定座標に移動    |
| `look-at`       | x, y, z        | 指定座標を見る    |
| `check-path-to` | x, y, z        | パス存在確認      |
| `jump`          | なし           | ジャンプ          |
| `set-sneak`     | enabled        | スニーク ON/OFF   |
| `set-sprint`    | enabled        | スプリント ON/OFF |
| `swing-arm`     | なし           | 腕を振る          |

---

## 🔨 ブロック操作（3 個）

| スキル           | 主要パラメータ     | 用途           |
| ---------------- | ------------------ | -------------- |
| `dig-block-at`   | x, y, z            | ブロック掘削   |
| `place-block-at` | blockName, x, y, z | ブロック設置   |
| `can-dig-block`  | x, y, z            | 掘削可能性確認 |

---

## 🔍 情報取得（15 個）

| スキル                 | 主要パラメータ                | 返り値例                              |
| ---------------------- | ----------------------------- | ------------------------------------- |
| `find-blocks`          | blockName, maxDistance, count | "iron_ore x3: (100,12,200)..."        |
| `get-block-at`         | x, y, z                       | "stone (ID:1)"                        |
| `is-block-loaded`      | x, y, z                       | "チャンクはロード済み"                |
| `check-inventory-item` | itemName                      | "iron_ingot: 5 個"                    |
| `list-inventory-items` | なし                          | "oak_log x32, stone x64..."           |
| `get-equipment`        | なし                          | "手: iron_sword, 頭: iron_helmet..."  |
| `find-nearest-entity`  | entityType, maxDistance       | "zombie: 距離 8.5m"                   |
| `list-nearby-entities` | maxDistance, maxCount         | "zombie(mob) 8.5m, cow 12.3m"         |
| `get-health`           | なし                          | "体力: 15/20, 空腹度: 18/20"          |
| `get-position`         | なし                          | "位置: (100.5, 64.0, 200.3)"          |
| `get-time-and-weather` | なし                          | "時刻: 18:00 (夜), 天候: 晴れ"        |
| `check-recipe`         | itemName                      | "必要材料: iron_ingot x3..."          |
| `wait-time`            | milliseconds                  | "3000ms 待機しました"                 |
| `find-structure`       | structureType                 | "fortress: (150,70,250)"              |
| `get-bot-status`       | なし                          | health, food, position 等まとめて取得 |

---

## ⚔️ 戦闘・防御（3 個）

| スキル                | 主要パラメータ          | 用途             |
| --------------------- | ----------------------- | ---------------- |
| `attack-nearest`      | maxDistance             | 単発攻撃         |
| `attack-continuously` | maxAttacks, maxDistance | 敵を倒すまで攻撃 |
| `set-shield`          | enabled                 | 盾ガード切替     |

---

## 🎒 アイテム・クラフト（6 個）

| スキル                | 主要パラメータ        | 用途                   |
| --------------------- | --------------------- | ---------------------- |
| `hold-item`           | itemName              | アイテム装備           |
| `drop-item`           | itemName, count       | アイテムドロップ       |
| `use-item`            | なし                  | 手持ちアイテム使用     |
| `use-item-on-block`   | x, y, z               | ブロックにアイテム使用 |
| `craft-one`           | itemName              | 1 個クラフト           |
| `pickup-nearest-item` | itemName, maxDistance | 最寄アイテム拾得       |

---

## 🗄️ コンテナ（6 個）

| スキル                    | 主要パラメータ                      | 用途               |
| ------------------------- | ----------------------------------- | ------------------ |
| `open-container`          | x, y, z                             | コンテナを開く     |
| `deposit-to-container`    | x, y, z, itemName, count            | チェストに収納     |
| `withdraw-from-container` | x, y, z, itemName, count            | チェストから取出   |
| `start-smelting`          | x, y, z, inputItem, fuelItem, count | かまどで精錬       |
| `check-furnace`           | x, y, z                             | かまど状態確認     |
| `pickup-nearest-item`     | itemName, maxDistance               | 地面のアイテム拾得 |

---

## 🌾 農業（3 個）

| スキル          | 主要パラメータ    | 用途         |
| --------------- | ----------------- | ------------ |
| `plant-crop`    | x, y, z, cropName | 作物を植える |
| `harvest-crop`  | x, y, z           | 作物を収穫   |
| `use-bone-meal` | x, y, z           | 骨粉使用     |

---

## 🏗️ 建築（1 個）

| スキル      | 主要パラメータ                    | 用途           |
| ----------- | --------------------------------- | -------------- |
| `fill-area` | x1, y1, z1, x2, y2, z2, blockName | エリアを埋める |

---

## 🐄 動物・村人（2 個）

| スキル                | 主要パラメータ       | 用途       |
| --------------------- | -------------------- | ---------- |
| `breed-animal`        | animalType, foodItem | 動物を繁殖 |
| `trade-with-villager` | tradeIndex, times    | 村人と取引 |

---

## 🌙 生存（2 個）

| スキル         | 主要パラメータ | 用途             |
| -------------- | -------------- | ---------------- |
| `eat-food`     | なし           | 食事（自動選択） |
| `sleep-in-bed` | x, y, z        | ベッドで寝る     |

---

## 🌐 次元移動（1 個）

| スキル         | 主要パラメータ | 用途           |
| -------------- | -------------- | -------------- |
| `enter-portal` | x, y, z        | ポータルに入る |

---

## 🔧 その他（2 個）

| スキル           | 主要パラメータ | 用途               |
| ---------------- | -------------- | ------------------ |
| `activate-block` | x, y, z        | ブロック右クリック |
| `chat`           | message        | チャット送信       |

---

## 💡 actionSequence の使い方

### 基本形式

```json
"actionSequence": [
  {
    "toolName": "find-blocks",
    "args": "{\"blockName\": \"oak_log\", \"maxDistance\": 50, \"count\": 3}",
    "expectedResult": "木を発見"
  },
  {
    "toolName": "move-to",
    "args": "{\"x\": 98, \"y\": 63, \"z\": 201, \"range\": 2}",
    "expectedResult": "木に近づく"
  },
  {
    "toolName": "dig-block-at",
    "args": "{\"x\": 98, \"y\": 63, \"z\": 201}",
    "expectedResult": "木を掘る"
  }
]
```

### 重要ルール

```
✅ args は JSON文字列形式（ダブルクォートのみ）
✅ 実数値のみ使用（プレースホルダー禁止）
✅ 2-10個の単純な操作に最適
✅ エラー時は即座にplanningに戻る
```

---

## 🎯 実現可能な高度な業務

### 1. 素手から鉄インゴット入手

```
木材入手 → クラフトテーブル作成 → 木のツール作成 →
石採掘 → 石のツルハシ作成 → 鉄鉱石採掘 →
かまど作成 → 燃料入手 → 精錬 → 鉄インゴット回収

使用スキル: 21種類、約60アクション
```

### 2. 夜の敵モブ対策

```
パターンA: ベッドで寝る
パターンB: 戦闘で撃退
パターンC: 緊急回避

状況に応じてLLMが選択
```

### 3. ネザーでブレイズロッド入手

```
ポータル作成 → 着火 → ネザー移動 →
要塞探索 → ブレイズ発見 → 撃破 → 帰還

使用スキル: 15種類、約30アクション
```

---

## 📝 スキル追加ガイド

新しいスキルを追加する場合：

```typescript
// backend/src/services/minebot/instantSkills/yourSkill.ts
export class YourSkill extends InstantSkill {
  name = "your-skill";
  description = "スキルの説明";

  params: SkillParam[] = [
    { name: "param1", type: SkillParamType.String, required: true },
  ];

  async runImpl(args: string[]): Promise<SkillResult> {
    // 実装
    return { success: true, result: "結果メッセージ" };
  }
}
```

エラーハンドリングレベル:

- **レベル 1**: try-catch のみ
- **レベル 2**: 事前チェック（パラメータ、距離、条件）
- **レベル 3**: 詳細なエラーメッセージ
